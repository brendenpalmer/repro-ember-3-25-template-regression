{{! Template from `ilios` with MIT license }}
{{! Repo URL: https://github.com/ilios/frontend }}
{{page-title (t "general.instructorGroups")}}
<InstructorGroups::Root
  @setTitleFilter={{perform this.changeTitleFilter}}
  @titleFilter={{this.titleFilter}}
  @setSchoolId={{set this.schoolId}}
  @schoolId={{this.schoolId}}
  @schools={{this.model}}
/>
