{{! Template from `ember-observer` with MIT license }}
<button type="button" class="button-option {{if this.isSelected 'selected'}}" {{on "click" (fn @sortBy @key)}} ...attributes>
  {{@name}}
</button>
