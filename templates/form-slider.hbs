{{! Template from `travis` with MIT license }}
{{! Repo URL: https://github.com/travis-ci/travis-web }}
<input 
  type='range' 
  min={{this.min}} 
  max={{this.max}} 
  step={{this.step}}
  value={{this.value}} 
  list={{this.list}}
/>
{{yield}}
