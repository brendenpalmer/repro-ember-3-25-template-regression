{{! Template from `ilios` with MIT license }}
{{! Repo URL: https://github.com/ilios/frontend }}
<ProgramYear::Header @programYear={{@model}} @canUpdate={{this.canUpdate}} />
<ProgramYear::Overview @programYear={{@model}} @canUpdate={{this.canUpdate}} />
{{outlet}}
