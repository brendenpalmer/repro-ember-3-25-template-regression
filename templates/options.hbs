{{! Template from `pinochle` with MIT license }}
{{!--
{{#if this.DEBUG}}
  {{this.poorMansEffect}}

  <label class='fixed:top-right grid:cols gap-2 align-items:center'>
    <input type='checkbox' {{on 'click' this.toggle}} checked={{this.isSynthwave}}>
    <span>Synthwave</span>
  </label>
{{/if}}
--}}