{{! Template from `ember-observer` with MIT license }}
<PageLayout @showCategories={{true}}>
  <div class="container login with-default-styling">
    <LoginForm @loginAction={{this.login}} />
  </div>
</PageLayout>
