{{! Template from `ilios` with MIT license }}
{{! Repo URL: https://github.com/ilios/frontend }}
{{page-title (t "general.myProfile")}}
<MyProfile
  @user={{@model}}
  @showCreateNewToken={{this.showCreateNewToken}}
  @toggleShowCreateNewToken={{toggle-action "showCreateNewToken" this}}
  @showInvalidateTokens={{this.showInvalidateTokens}}
  @toggleShowInvalidateTokens={{toggle-action "showInvalidateTokens" this}}
/>
