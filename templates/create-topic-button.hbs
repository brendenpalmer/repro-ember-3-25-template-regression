{{! Template from `discourse` with MIT license }}
{{#if canCreateTopic}}
  {{d-button
    class=btnClass
    id="create-topic"
    action=action
    icon="plus"
    disabled=disabled
    label=label}}
{{/if}}
