{{! Template from `pinochle` with MIT license }}
{{#if @players}}

  <ul class='list:flat'>
    {{#each @players as |player|}}
      <li>
        {{player.name}}

        {{#if (eq @currentName player.name)}}
          &nbsp;(you)
        {{/if}}
      </li>
    {{/each}}
  </ul>

{{/if}}

