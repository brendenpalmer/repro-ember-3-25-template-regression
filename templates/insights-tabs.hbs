{{! Template from `travis` with MIT license }}
{{! Repo URL: https://github.com/travis-ci/travis-web }}
{{#each this.tabs as |tab|}}
  <li class="insights-tab">
    <LinkTo @query={{hash timeInterval=tab.slug}} data-test-insights-period={{tab.slug}}>
      {{tab.title}}
    </LinkTo>
  </li>
{{/each}}
